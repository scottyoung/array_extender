Overview:

Extends default operations on a JavaScript array.

Adds two new operations:
	.addAll() - returns the sum of all values in the array
	.csv() - returns all values of the array in CSV format




Credit:

Scott Young 2012 snyoung@gmail.com